block('dropdown')(
    def()(function() {
        var ctx = this.ctx,
            id = this.generateId();

        applyCtx([
            { elem : 'switcher', content : ctx.switcher, id : id },
            { elem : 'popup', popup : ctx.popup, id : id }
        ]);
    })
);
